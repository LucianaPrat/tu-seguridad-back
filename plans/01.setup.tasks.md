# 01 — Task status tracker

> Status snapshot for `plans/01.setup.md`. Update as tasks progress.

| Task | Status | Notes |
|---|---|---|
| T01 — Scaffold NestJS 11 | ✅ Done | Committed `chore: scaffold nestjs 11 project with strict typescript`. `.nvmrc` (22), `.editorconfig` present. |
| T02 — Config module + Joi env validation | ✅ Done | Committed `feat: config module with joi env validation`. `EnvNames`, `envValidationSchema` in `src/cross/config|common`. |
| T03 — Bootstrap main.ts | ✅ Done | Committed `feat: hardened http bootstrap with pino logging`. helmet/cors/pino wired; `src/observability/tracing.ts` stub in place for T18. |
| T04 — Either pattern + global interceptor | ✅ Done | Committed `feat: either error handling with global interceptor`. Tests in `either.spec.ts` / `either.interceptor.spec.ts`. |
| T05 — Swagger | ✅ Done | Committed `feat: swagger with bearer auth`. `src/cross/config/swagger.config.ts`. |
| T06 — Throttling (inbound) | ✅ Done | Committed `feat: global inbound throttling, production only`. |
| T07 — Prisma: schema, migration, seed | ✅ Done | Committed `feat: prisma schema, initial migration and admin seed`. Migration applied to `tu-seguridad` and `tu-seguridad-test` (docker container `mysql-local`, port 3306); seed verified idempotent (2 runs → 1 admin row); build + boot smoke clean (`Prisma connected` logged, no errors). |
| T08 — Data accessors | ✅ Done | Committed `feat: data accessor layer over prisma`. 5 accessors (`user/camera/zone/zone-event/hit`) in `src/data/accessors/`, exported by `DataModule` (`PrismaService` now also exported for future T10/T19 use). `npm run test:int` (new script, real MySQL test DB) green: 5 suites, 15 tests, incl. Camera→Zone FK restrict. `npm test` unchanged (17 tests, int-specs correctly excluded). Build/lint clean. |
| T09 — Auth module (JWT) | ✅ Done | Committed `feat: jwt auth with refresh against db users`. `AuthModule` (`src/modules/auth/`): login/refresh against `UserAccessorService`, access+refresh signed with distinct secrets/TTLs from env. `JwtAuthGuard` (`src/cross/guards/`) global via `APP_GUARD`, bypassed by `@Public()` (`src/cross/decorators/`); `@CurrentUser()` reads `request.user`. Cross-token misuse rejected both ways (refresh rejected by access guard via secret mismatch + explicit `type` check; access token rejected by `refresh()` via `type` check). `npm test -- auth` green (12 tests). Manual curl: login 200 both tokens, wrong password 401 envelope, protected route 401 without/200 with Bearer, refresh rotation 200, garbage/cross-misuse tokens 401, `/docs` still public. `/api/v1/cameras` 401→200 check deferred to T11 (module doesn't exist yet). |
| T10 — Health checks | ✅ Done | Committed `feat: terminus health endpoints with db readiness`. `HealthModule` (`src/modules/health/`): `HealthController` at version-neutral `health` path (`@Controller({path:'health', version: VERSION_NEUTRAL})` — needed because URI versioning injects `/v1/` even on routes excluded from the `api` prefix; without it `/health/live` 404s). `PrismaHealthIndicator` does `SELECT 1` via `PrismaService` using the modern `HealthIndicatorService` API (T09's `@Public()` used on both routes). `npm test` green (33 tests, 2 new suites). Manual: `/health/live` 200, `/health/ready` 200 `db:up`; `docker stop mysql-local` → `/health/ready` 503 `db:down` with error message, `/health/live` stayed 200; `docker start mysql-local` restored `db:up` within seconds. |
| T11 — Cameras module | ✅ Done | Committed `feat: cameras crud module`. `CamerasModule` (`src/modules/cameras/`): CRUD via `CameraAccessorService`, duplicate id → `CONFLICT`, missing → `NOT_FOUND`, delete-with-zones → `CONFLICT`. List masks `snapshotUrl` as `"***"` (`camera.mapper.ts`); detail GET returns it in full. `CameraStatusRegistry` in-memory singleton (all nulls until T16) backs `GET /cameras/:id/status`. Also added a global `ValidationPipe.exceptionFactory` (`src/cross/errors/validation-exception.factory.ts`) so class-validator failures return the project's `{code:"VALIDATION_ERROR",...}` envelope instead of Nest's default shape — gap existed since T02/T03, first observable once real DTOs landed. `npm test` green (45 tests, 12 new). Manual curl: create 201, duplicate 409, `confidenceThreshold:2` → 400 `VALIDATION_ERROR` naming the field, list masks url, detail shows it, status stub all-null, update 200/404, delete 200 then 404 on re-fetch. |
| T12 — Zones module + polygon validation | ✅ Done | Committed `feat: zones crud with polygon geometry validation`. Pure `src/modules/zones/geometry.ts` (no Nest deps): `validatePolygon` checks min-vertices, [0,1] bounds, consecutive duplicates (ε=1e-9), shoelace min-area (1e-4), and O(n²) proper segment-intersection (adjacent edges skipped). Routes match §7 exactly: `POST/GET /cameras/:cameraId/zones` (nested, cameraId from path not body) and `GET/PUT/DELETE /zones/:id` + `POST /zones/:id/validate` (single `ZonesController`, no controller-level prefix). `PUT` bumps `geometryVersion` only when `polygon` is present in the body; `/validate` always returns 200 (`{valid,violations}`), even for a bad override polygon — only a missing zone id (no override given) is a real 404. `geometry.spec.ts`: 10 cases (the plan's 9 + one extra to close a coverage gap on the collinear-touch branch of the intersection check) — `npm test -- --coverage --collectCoverageFrom='modules/zones/geometry.ts' zones/geometry` → 100% lines. `npm test` green (68 tests, 23 new). Manual curl: valid zone 201, bowtie 400 `INVALID_POLYGON` message contains "Polygon edges self-intersect", validate dry-run with bad override → 200 `valid:false`, update polygon → `geometryVersion` 1→2, update name-only → version unchanged, delete then camera cleanup all 200. |
| T13 — Events module (query + WS gateway) | ✅ Done | Committed `feat: zone events query and websocket broadcast`. `GET /events` with cameraId/zoneId/eventType/from/to/limit filters via `ZoneEventAccessorService.query`; limit silently clamped to `[1,1000]` (no validation error) rather than default-100. `EventsGateway` (`/events` namespace): handshake guard verifies JWT from `client.handshake.auth.token`, disconnects on missing/invalid token; CORS via a new `SocketIoAdapter` (`src/cross/config/`) wired in `main.ts` so the same env-driven origin allowlist covers WS, keeping env access confined to bootstrap. `EventsService.emit()` is idempotent on `eventId` (added `ZoneEventAccessorService.findByEventId`) — the single entry point T16's pipeline will call. `npm test -- events` green (10 tests) including a real `socket.io-client` gateway test (bad/no token disconnected, valid token receives a broadcast `zone-event` well under 1s). `npm test` green (78 tests, 10 new). Manual: curl 401 without token, `?limit=5000` returns 200 (no error), bad `eventType` enum → 400 `VALIDATION_ERROR`; live WS check script confirmed bad/no-token clients get disconnected and a valid-token client stays connected against the actual running app (not just the isolated gateway test). |
| T14 — Occupancy engine | ✅ Done | Committed `feat: occupancy engine with point-in-polygon and hysteresis`. `pointInPolygon` added to `geometry.ts` (ray-casting, boundary-inclusive via the existing `orientation`/`onSegment` helpers — a point on an edge or vertex counts as inside, matching Shapely `covers()`). `src/modules/pipeline/occupancy.engine.ts`: pure `OccupancyEngine` class (no Nest deps, no I/O), one `Outside\|CandidateInside\|Inside\|CandidateOutside` state per `(cameraId, zoneId)`, `enterConsecutivePolls`/`exitConsecutivePolls` as constructor params (defaulting to `PipelineDefaults`) rather than DI, so tests instantiate it directly with small numbers. A miss during `CandidateInside` (or a hit during `CandidateOutside`) resets the streak to 0 rather than decrementing — this is what makes a flicker (in,out,in) not fire. `evaluate()` skips disabled zones entirely (no state stored); `reset(cameraId)` drops all of that camera's zone state for zone-edit/camera-disable. `npm test -- occupancy` (8 tests) and `npm test -- zones/geometry` (14 tests, +4 for `pointInPolygon`) green; both `geometry.ts` and `occupancy.engine.ts` at 100% lines via `--collectCoverageFrom`. `npm test` green (90 tests, 12 new). No app wiring yet — pure domain logic consumed by T16's pipeline; no manual curl step in this task's DoD. |
| T15 — face-auth detect client | ✅ Done | Committed `feat: face-auth persons detect client`. `FaceAuthClientModule`/`FaceAuthClientService` (`src/modules/face-auth-client/`): `detectPersons(image, filename)` POSTs multipart to `{FACE_AUTH_API_URL}/api/v1/persons` with `Fa-Domain`/`Fa-Token` headers, `DETECT_TIMEOUT_MS` timeout. Errors mapped: `ECONNABORTED`/timeout message → `UPSTREAM_TIMEOUT`; any other Axios error → `UPSTREAM_ERROR` with the upstream HTTP status in the message, never the token (asserted in tests). Wrapped in `withSpan('face-auth.detect', …)` — added the no-op stub `src/observability/tracing.helpers.ts` now (T18 swaps the implementation, call sites unchanged). Registered in `AppModule` (no controller/routes of its own) so the manual script and T16 can both resolve it via the same DI graph. `npm test -- face-auth` green (6 tests: success mapping, timeout, 500, 429, non-axios error, token-never-leaks). `npm test` green (96 tests, 6 new). `scripts/try-detect.ts` included per DoD; sanity-checked it boots and exits cleanly on the no-arg path — the live-API step itself needs real `FACE_AUTH_DOMAIN`/`FACE_AUTH_TOKEN` creds and a test image, neither available in this environment, so that specific manual check is left for whoever has real face-auth tenant credentials. |
| T16 — Detection pipeline | ✅ Done | Committed `feat: detection pipeline with dvr polling and manual analyze`. `PipelineModule` (`src/modules/pipeline/`): `SnapshotService` (fetches DVR image, timeout+content-type checks, URL never logged), `PipelineService.processImage` (detect → confidence filter → `OccupancyEngine.evaluate` → `EventsService.emit` per transition → `CameraStatusRegistry.record`), `PollingScheduler` (`@nestjs/schedule`, per-enabled-camera dynamic intervals, 30s resync for camera CRUD changes, in-flight skip counted via `incrementSkipped`, `POLLING_ENABLED=false` logs `"polling disabled"` and registers nothing). Extracted `CameraStatusRegistry` into its own `@Global()` `CameraStatusModule` to break a `CamerasModule`↔`PipelineModule` cycle (Cameras needs Pipeline for `/analyze`; Pipeline needs the registry). `POST /cameras/:id/analyze` added (multipart, 10MB cap via `ParseFilePipeBuilder`, needed `@types/multer`). **Side fix**: replaced the ESM-only `uuid` dep in `logger.options.ts` with `node:crypto`'s `randomUUID` — first-ever full-`AppModule` integration test (see below) exposed that `uuid` broke Jest's default CJS transform; removed the now-unused dependency. `npm test -- pipeline` and `-- occupancy` etc. green (113 tests total, 17 new: snapshot/pipeline/scheduler unit specs). New `pipeline.analyze.int-spec.ts` (real test DB + real HTTP + real WS via a fake `FaceAuthClientService`, `test:int` green, 17 tests) proves the full loop end-to-end: analyze → persons+zoneResults every call, `eventsEmitted` empty until hysteresis confirms on the 2nd poll, the confirmed `zone-event` actually arrives over a connected `socket.io-client`, and `GET /status` reflects `lastSuccessAt`/`lastPersonsDetected`. Manual: live boot with `POLLING_ENABLED=false` logs `"polling disabled"`; live boot with `true` + a real camera created via curl booted clean with no crash (30s resync cadence means a freshly-created camera's first tick isn't observed within a few seconds — covered precisely by the `syncIntervals`/`pollOnce` unit tests instead). Real face-auth creds still unavailable in this environment, same caveat as T15. |
| T17 — Hit metering interceptor | ✅ Done | Committed `feat: hit metering interceptor persisted to db`. Global `HitInterceptor` (`src/cross/interceptors/`) hooks the raw Express response's `finish` event (not the RxJS pipe) so the recorded `statusCode` is always the true final one regardless of which interceptor/filter produced it; fire-and-forget `HitAccessorService.create(...)`, sink failures caught and logged `warn`, never affect the response. Skips `/health*` and `/docs*`. `userId` read from `request.user.sub` (populated by `JwtAuthGuard`, which runs before interceptors) when present. `npm test -- hit` green (4 tests: one-hit-per-request incl. userId, 4xx/5xx→isError, health/docs skipped, sink-failure doesn't break the response). `npm test` green (117 tests, 4 new). Manual: live boot, login + 2 authenticated calls → 3 `Hit` rows (verified via a direct Prisma query) incl. correct `userId`; `/health/live` and `/docs` produced zero rows; test rows cleaned up after. |
| T18 — OpenTelemetry + withSpan | ✅ Done | Committed `feat: opt-in opentelemetry tracing with withSpan helper`. `tracing.ts`: no-op unless `OTEL_ENABLED==='true'`, else `NodeSDK` with OTLP HTTP trace exporter (URL resolved from `OTEL_EXPORTER_OTLP_ENDPOINT` by the exporter itself, no hand-rolled path-joining), service name from `OTEL_SERVICE_NAME`, auto-instrumentations with fs/dns disabled, SIGTERM/SIGINT shutdown. Needed an explicit `import 'dotenv/config'` at the top of this one file — it runs before `ConfigModule.forRoot()`, the normal place `.env` gets loaded, so without it `OTEL_ENABLED` would never be seen in dev. `tracing.helpers.ts`: real `withSpan` via `@opentelemetry/api`'s `trace.getTracer(...).startActiveSpan(...)` — deliberately does *not* branch on `OTEL_ENABLED` itself, since the API package returns a no-op tracer automatically when no SDK registered a real provider (i.e. when disabled), so the no-op/real split is structural, not an env check. `npm test -- tracing` green (3 tests: returns result, propagates async rejection, propagates sync throw). `npm test` green (120 tests, 3 new). Manual: `OTEL_ENABLED=true` with no collector at `localhost:4318` → app boots clean, serves requests normally, no crash (exporter failures are non-fatal by OTel design); `OTEL_ENABLED=false` → zero OTel-related output, confirmed trivially since every other live-boot check this session ran with it disabled and never printed anything OTel-related. |
| T19 — Testing harness + e2e suite | ✅ Done | Committed `test: e2e suite and 3-level test scripts`. Removed the default scaffold `app.e2e-spec.ts`. `test/utils/`: `bootstrap-e2e-app.ts` (boots the real `AppModule` end-to-end — HTTP+WS+test DB — replicating `main.ts`'s prefix/versioning/pipes/**Swagger setup**, since none of that lives on `AppModule` itself; overrides `FaceAuthClientService` with a fake; also upserts the seeded admin directly so the suite doesn't depend on `prisma:seed` having run against `DATABASE_URL_TEST`), `auth-as.ts`, `truncate-all.ts`, `typed-body.ts` (casts supertest's `any`-typed `.body` — needed once real assertions on response shapes started, mirrors the same fix already applied in T16's int-spec). 5 e2e spec files (auth+docs+health, cameras, zones, events, analyze-with-WS) — 16 tests. Scripts updated to the plan's exact 3-level shape (`test`/`test:cov` now explicitly ignore int/e2e specs, added `test:all`); `test:int` deliberately kept on its existing dedicated config (not the plan's simplified inline form) since that's what actually forces `DATABASE_URL=DATABASE_URL_TEST` — swapping it would've silently broken every int-spec. `collectCoverageFrom` now excludes module/dto/test files (boilerplate with no branching logic); added 5 more unit specs (4 controllers + `CameraStatusRegistry`) to close the resulting gap — real delegation-correctness tests, not coverage-gaming. `npm run test:cov` → 84.48% lines (`coverageThreshold.global.lines: 80` now enforced). `npm run test:all` (unit 140 + int 17 + e2e 16 = 173 tests) fully green. `jest --detectOpenHandles` on e2e clean — no leaked sockets/Prisma connections. |
| T20 — Debugger configuration | ✅ Done | Committed `chore: vscode debugger configurations`. `start:debug` now `nest start --debug 0.0.0.0:9229 --watch`. `.vscode/launch.json`: "Attach to API" (attach, port 9229, `restart:true`, `sourceMaps:true`) and "Debug Jest current file" (launch, `node_modules/.bin/jest ${fileBasename} --runInBand`, integrated terminal). Manual: `npm run start:debug` printed `Debugger listening on ws://0.0.0.0:9229/<uuid>` exactly. Verified breakpoint bind-and-halt behavior by scripting the Chrome DevTools Protocol directly (headless environment, no VS Code GUI available) rather than trusting the config blind: confirmed a breakpoint set on the global `HitInterceptor` — which runs on every request, including camera routes — genuinely paused live V8 execution on an incoming curl and resumed cleanly afterward. (An earlier attempt targeting `CamerasController.findAll()` specifically never paused despite reporting a bound location; root-caused to the diagnostic script's own `scriptId` matching, not the debug setup — ruled out via `ps`/`ss` process-identity checks and a watch-mode-restart check, both clean.) Also confirmed `node --inspect-brk node_modules/.bin/jest geometry.spec.ts --runInBand` — the exact command the Jest launch config runs — attaches a working inspector, validating that config's mechanics. |
| T21 — PM2 production setup | ⬜ Not started | No `ecosystem.config.js`. |
| T22 — Git hooks (husky/lint-staged/commitlint) | ⬜ Not started | No husky/commitlint deps. |
| T23 — Documentation (README/AGENT.md/CLAUDE.md) | ⬜ Not started | `README.md` is still the default Nest CLI placeholder; `AGENT.md`/`CLAUDE.md` missing. |
| T24 — CI (GitHub Actions) | ⬜ Not started | No `.github/workflows/`. |
| T25 — Final review & push | ⬜ Not started | Repo has no commits pushed to `origin` yet (`git ls-remote --heads origin` empty). |

## Legend
✅ Done and committed · 🟡 In progress / uncommitted · ⬜ Not started

## Deviations from plan so far
- `schema.prisma` adds `shadowDatabaseUrl` (`SHADOW_DATABASE_URL` env var) beyond the plan's §6 list — used for `prisma migrate dev` shadow DB, not in original env spec but validated in Joi as optional.
