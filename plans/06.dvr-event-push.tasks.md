# 06 — Task status tracker

> Status snapshot for `plans/06.dvr-event-push.md`. Update immediately after
> finishing a task — what was built, how it was verified — before starting the next one.

| Task                                              | Status                            | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------- | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T01 — Sign the recorder request with its own verb | ✅ Done and committed (`65a3895`) | `buildAuthorization` takes the method and builds HA2 as `METHOD:uri`; `get()` became `request(connection, method, path, overrides)` and the three `httpService.get` calls became `httpService.request`. A `discard` helper destroys a `Readable` body on both paths that abandon a `401`, which was free for the buffered callers and a leaked socket per reconnect for a streaming one — fixed on the shared function, not on the future caller. Spec migrated to a `{ request: jest.Mock }` mock and an `AxiosRequestConfig[]` accessor: 21 references, 6 positional reads. No behaviour change for existing readers.                                                                                              |
| T02 — `linkMotionEvents` on the port              | ✅ Done and committed (`d451562`) | Fourth port method and the first that writes to the appliance. GET → return `alreadyLinked` if `center` is present → splice before the last closing tag → PUT → re-GET to confirm. `tagText` deliberately not reused for the presence check (it returns the first match and there are several `notificationMethod` tags); a `matchAll` helper does it. A body that is not a notification list is refused with **no PUT issued**, which one of the six tests asserts directly. `ponytail:` note left on the self-closed-empty-list case, never seen on V4.71.410.                                                                                                                                                     |
| T03 — `POST /api/v1/dvr/event-linkage`            | ✅ Done and committed (`d451562`) | Admin only, mirrors `rediscover`. Serial over the recorder's own roster — the digest challenge cache carries one mutating nonce counter per recorder, and these are writes to security hardware. Best effort with a per-channel report, because a recorder offers no transaction and "six of eight worked" without naming the other two is not actionable. Stops on the first `UPSTREAM_TIMEOUT` rather than spending seven more. Writes no `lastTestAt`/`lastTestOk`, same posture as `testConnection`. `openapi.json` regenerated; `outcome` carries an explicit enum, since a TS string-literal union emits as a bare `String`.                                                                                   |
| T04 — `DvrEventListener`                          | ✅ Done and committed (`01c664b`) | One detached supervisor per recorder, so a dead one stalls nothing. Watchdog armed **before** the connect, so one timer covers a black-holed TCP connect, headers that never arrive and a socket gone quiet — and it is fed by every notification, because the recorder's idle beat pauses while it is busy reporting. Debounce keyed per camera and stamped synchronously with its check. The camera lookup is awaited inside the read loop on purpose (a detached resolve would let two pulses 10 ms apart both pass); the poll deliberately is not (awaiting it stops reading the socket). `pollGuarded` made public rather than adding a method. Watchdog and backoff both unref'd. 18 tests, suite exits clean. |
| T05 — Configuration                               | ✅ Done and committed (`01c664b`) | Three `DVR_EVENTS_*` vars. Debounce floor is `1` and not `0`: the in-flight guard bounds a camera to roughly one capture every 1.5 s, faster than the detection rung, and `0` against the IP-throttled detector is the `429` storm of `plans/05` §2.5. `POLLING_PASSIVE_SECONDS` made conditional on `DVR_EVENTS_ENABLED` (15 off / 300 on) rather than flat 300 — verified working on Joi 18.2.3 including env string coercion — because a flat change would regress any deployment that upgrades without turning events on. Reconnect timing stayed a module constant; the idle window earned a var because it tracks a heartbeat the recorder chooses.                                                            |
| T06 — Documentation                               | ✅ Done and committed             | README stack row, detection-pipeline sentence, cadence table and a `Recorder-pushed events` section stating that the base tick is still 5 s; roadmap bullet rewritten in place to "Decided: both" rather than deleted; `ARCHITECTURE.md` bullet on why the poll survives plus the shutdown ordering; `docs/BEST_PRACTICES.md` operator runbook including the two axios findings and the empty-socket trap; this plan and tracker.                                                                                                                                                                                                                                                                                    |

## Legend

✅ Done and committed · 🟡 In progress / uncommitted · ⬜ Not started · ⚠️ Needs a manual/owner step
that cannot run from CI or dev

## Verification run on the whole change

- `npm run lint` — clean, `--max-warnings 0`.
- `npm run typecheck` — clean.
- `npx prettier --check` — clean on every touched `.ts`. `README.md`, `ARCHITECTURE.md` and
  `docs/BEST_PRACTICES.md` were already failing prettier on `develop`; this change leaves them no
  worse, confirmed against a stashed baseline.
- `npm test` — **712 passed, 73 suites**, up from 670 on `develop`.
- `npm run build` — clean.

### Against the real recorder

Driven through the compiled adapter, not curl, so the code under test is the shipped code.

- `discoverChannels` returned `1:offline 2:offline 3:online 4:online 5:online 6:offline 7:online
8:online` — matching the ports actually wired.
- `linkMotionEvents('4')` answered `alreadyLinked` and issued no write; `'99x'` was refused
  `VALIDATION_ERROR` before any request.
- `openEventStream` opened, classified the idle beats as `keepalive` at roughly 9.5 s, and aborted
  cleanly on signal.
- The write branch was exercised by stripping `center` from channel 2 and calling the method:
  `linked`, then `center` present again **and `record-2` and `whiteLightOut-2` both intact** — the
  regression a composed document would have caused — then `alreadyLinked` on the repeat. That run is
  also the real-hardware proof of T01: without the verb in HA2 the PUT would have come back as a
  credential rejection.

### Still owed

See [`docs/DVR_EVENTS_FOLLOWUP.md`](../docs/DVR_EVENTS_FOLLOWUP.md).
