# 07 — Task status tracker

> Status snapshot for [`plans/07.playback-backfill.md`](07.playback-backfill.md). Update immediately
> after finishing a task — what was built, how it was verified — before starting the next one.
>
> **Work paused 2026-09-10.** Everything needed to resume is in this file and the documents it links.

| Task                                            | Status                               | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------------------- | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T00 — Does sampling more frames beat one frame? | 🟡 In progress · 1 of ≥10 events · sampling is now ~6× cheaper | First measurement done and committed: [`docs/T00-frame-window-measurement.md`](../docs/T00-frame-window-measurement.md), raw TSV in `docs/data/`. On channel 8's 2026-09-09 23:11:27 event, 6 of 39 answered frames detected the person — **5 of 13 before** the notification, **0 of 25 after**, and the three best scores (0.809 / 0.883 / 0.847) five seconds _before_ the recorder published. The live capture succeeded on a 0.670 with about half a second of margin. Pre-registered decision rule met, **on n=1**. Nine more events needed, another camera, some daylight, and at least two where the live capture found nothing. **The corollary is now done:** `scripts/try-detect.ts` floors at 1.5 s and defaults to 2 s instead of 5/12, so a 40-frame event costs ~80 s rather than eight minutes — which is what makes nine more affordable. |
| T00a — Historical motion index                  | ⚠️ Blocked, superseded in practice   | `POST /ISAPI/ContentMgmt/logSearch` refused four different body shapes with `Invalid XML Content` / `two root tags` (with and without the XML prolog, with and without `metadataList`, with and without `majorType`). **Unverified, not disproven.** No longer on the critical path: T02a's overnight log supplies 187 timestamped events with channel ids, which is a better sampling source than the device log would have been.                                                                                                                                                                                                       |
| T01 — Recall ledger                             | ✅ Done                              | Built and committed. `SNAPSHOT_KEEP_MISSES` (off by default) stores the untouched frame of an event-triggered poll the detector answered `empty`; `filtered` stores nothing. `snapshots.reason` is a nullable enum (`ledger_miss`, `raw_copy`) with an index on `(reason, captured_at)`; the sweep is unchanged and still prunes ledger rows on the ordinary window. `scripts/export-ledger.ts` writes a date range out for `scripts/try-detect.ts`. Four decisions the plan left open are recorded below.                                                                                                                        |
| T02a — Event volume                             | ✅ Done                              | [`docs/T02a-event-volume-measurement.md`](../docs/T02a-event-volume-measurement.md), from 6.7 h of `alertStream`. Corrected three of the plan's own estimates — see below.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| T02b — Backfill window                          | ⬜ Not started · **redesign needed** | The plan proposed deriving the window from a burst's first and last pulse. T02a killed that: the **median burst is one event** (singletons 24/40, 36/58, 10/11). Use a fixed asymmetric window instead — T00 recommends `[T − 10 s, T + 2 s]`, which caught all six detections in 24 frames rather than 40 — and let a real burst widen it.                                                                                                                                                                                                                                                                                              |
| T02c — Backfill queue                           | ⬜ Not started · **redesign needed** | Also overturned by T02a. The slow lane was designed to protect a 1 req/s budget; at night the whole estate produces ~3.4 events/hour, so a backfill costs ~0.023 req/s and needs no protection at all. The constraint is **daytime** — the 18:00 hour alone produced 109 events, ~0.73 req/s at 24 frames each. Replace the permanent slow lane with a per-hour cap.                                                                                                                                                                                                                                                                     |
| T03 — face-auth batch endpoint                  | 🟡 Asked, they are building it       | Request sent: [`docs/face-auth-batch-endpoint-request.md`](../docs/face-auth-batch-endpoint-request.md). Two contract details already relayed and folded into that document (success is `201`; `stopOnFirstDetection` cuts at a chunk of 8). **Do not write the client method against an unshipped contract.**                                                                                                                                                                                                                                                                                                                           |
| T04 — Decide `targetType`                       | ⬜ Not started                       | Data now exists in T02a, but it does not separate `human` from `vehicle` events — the notification does not carry the class. Needs either a second measurement that records it, or a decision made on other grounds. `human` alone is verified to persist.                                                                                                                                                                                                                                                                                                                                                                               |
| T05 — Alert on silent degradation               | ⬜ Not started · **spec sharpened**  | T02a produced a useful negative: channels 5 and 7 fired **zero** events in 6.7 h with configuration identical to the three that did. A silent-degradation alert keyed on "no events" would have fired falsely on both. It has to compare a channel against **its own history**, not against zero.                                                                                                                                                                                                                                                                                                                                        |

## Legend

✅ Done and committed · 🟡 In progress · ⬜ Not started · ⚠️ Blocked or needs an owner step

---

## Decisions taken while building T01

The plan named the shape but left four things open. What was chosen, and what would reverse it.

| Decision                                                                                                                         | Reversed if                                                                                                                                        |
| -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `reason` is a Prisma enum, not a free string, and it tags the `SNAPSHOT_KEEP_RAW` copy as `raw_copy` as well as the ledger        | A third kind of audit row appears often enough that a migration per value is friction. Both values were named in the plan's own problem statement. |
| The metric is a `trigger` label on `pipeline_persons_detected_total`, not a new counter                                          | A dashboard needs ledger rows *written* rather than ledger candidates *seen* — the label counts the second, and the two differ only by the switch. |
| The trigger is threaded as a `PollTrigger` parameter through `pollGuarded`/`pollOnce`/`processImage`                             | A second thing needs to know the trigger, at which point it belongs on the captured image or on a request-scoped context instead of the signature. |
| The ledger's consumer is `scripts/export-ledger.ts`, not an HTTP route                                                          | Someone other than a developer at a terminal needs the frames. A route means auth, paging and a blob response for an audit tool with one user.     |

**Not done, deliberately:** the end-to-end walk-past check the plan's Verification section asks for
(turn the switch on, walk past a camera at night until the detector misses, confirm the row lands and
that `try-detect` reproduces the miss on the exported frame). It needs the recorder and a person
outdoors. Everything up to it is verified: 727 unit, 78 integration, 111 e2e.

---

## Where the work stands

**Merged:** [`plans/06`](06.dvr-event-push.md) entirely, PR #97 → `develop` at `bd592f5`. Recorder-pushed
motion events are live and verified end to end in the owner's deployment. PR #98 — the two
measurement documents (T00, T02a) plus the raw TSV — merged at `513377b`.

**Open:** PR #99, `scripts/try-detect.ts` paced against the measured 1 req/s limit. PR #100, T01.

**Note for whoever runs the suites next:** the local test database had a schema but no
`_prisma_migrations` table, so `migrate deploy` refused it with `P3005`. Baselining the seven
existing migrations with `prisma migrate resolve --applied` fixed it without a reset. The two
`analyze.e2e-spec` failures recorded as local-only were that same stale database — all 111 e2e pass
locally now.

**Uncommitted:** nothing. Two untracked HTML diagrams in `docs/` predate this work.

---

## The three numbers that matter most

1. **The good frames are before the notification.** Classifier lag ~6 s, and on the one event measured
   every detection after the event scored zero across 25 consecutive frames.
2. **Night volume is tiny.** ~3.4 events/hour for the whole estate between 21:00 and 00:12. The plan's
   "~30 per camera per night" conjecture was about 5× too high.
3. **The median burst is one pulse**, not the four-to-five seen during a walk test.

## Open questions, in priority order

1. **Does the reframe hold beyond one event?** T00 is n=1 and that event was a _success_ for the
   current design. A sample of successes cannot measure what the current design misses.
2. **Did channels 5 and 7 really see nothing for seven hours**, or is the recorder's classifier missing
   what they see? Three weeks of recording are available to check. If it is the latter, event-driven
   capture is blind on those cameras and no metric would show it.
3. **Is ffmpeg present on the production host?** It would be the first process spawn in this codebase
   — there is no `child_process` anywhere in `src/`, no Dockerfile, no provisioning script, and
   `docs/decisions/002-hls-live-streaming.md:111` rejected ffmpeg once already.
4. **`ALERT_COOLDOWN_SECONDS`** — the owner set `.env.example` to `60` during this session and the edit
   was lost in a rebase. Schema default and README both still say `0`. If `60` is wanted, all three
   move together.

## How to resume

Everything operational is in [`docs/BEST_PRACTICES.md`](../docs/BEST_PRACTICES.md) under
"DVR motion events" and "Measuring against recorded video".

T01 is built, so what is left is measurement, and it needs the recorder and the owner's machine:

1. **Nine more T00 events.** Pick timestamps and channels from the overnight log
   (`~/vmd-night.log`, 187 events); ch3 and ch4 carry most of the volume and T00 has not touched
   either. Follow the playback recipe, then `npx ts-node scripts/try-detect.ts out/` — the gap
   argument is no longer needed. Two of the nine should be events where the live capture found
   nothing; a sample of successes cannot measure what the current design misses.
2. **Then the ledger, end to end.** `SNAPSHOT_KEEP_MISSES=true`, walk past a camera at night until
   the detector misses, `npx ts-node scripts/export-ledger.ts /tmp/ledger`, and confirm
   `try-detect` reproduces the miss on the exported frame.

Once T00 has its ten events the decision rule can be applied for real, and T02b/T02c can be
redesigned against it — both still need the ffmpeg question answered first (open question 3).
